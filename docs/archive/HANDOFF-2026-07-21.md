# HANDOFF — sessione 2026-07-21 (Claude)

> Stato per la prossima sessione. NON riscrive i doc: li linka. Verità dal codice.
> Doc di riferimento: `PROBLEM-REGISTER-2026-07-21.md` (inventario+stato),
> `DESIGN-CLOUD-MEMORY-2026-07-21.md` (piano cloud/bridge/stimoli + action items),
> `TEST-OPENCODE-2026-07-21.md` (test plan locale), `AUDIT-2026-07-21.md` (run OpenCode),
> `GRAY-MATTER-COMPENDIUM.md` §0d (SSOT), `ENVIRONMENT.md` (regole sandbox vs locale).

## AGGIORNAMENTO sessione successiva (2026-07-21, Fable)
**B-STORE CHIUSO** (i 3 PENDING sotto sono fatti):
- `tests/test_bridge_promotion.py` riscritto su API (`all_bridges()`, env → `.db`,
  `promoted` truthy) + nuovo `test_migrates_legacy_json_once`.
- `pyproject.toml`: extra `cloud = ["libsql-client>=0.3.1"]`.
- Suite GM in sandbox: **40 passed, 3 skipped** (skip `mcp` attesi). Verde.

**Passo 2 (env model) FATTO**: nuovo `gray_matter/_env.py` (keep-in-sync con
`neuron/_env.py`): il daemon carica `<GM_HOME>/.env` in `__init__` (prima di
`bridges.py` e dei worker); i worker ereditano l'env → un solo `.env` GM porta
`TURSO_*`/`NEURAG_TURSO_*`/`GM_TURSO_*`. Override `GM_ENV_FILE`, opt-out
`GM_NO_DOTENV`, no-op sotto pytest, env reale vince. Test: `test_env_loader.py`
(4 test). Docs: `CONFIGURATION.md(.it)` aggiornati (tabelle GM/NeuRAG + nota env model).
Nota residua: `webgui.turso_save` scrive ancora `.env` in cwd (Neuron) — da
puntare a `<GM_HOME>/.env` quando si fa il pannello cloud (passo 3).

**Passo 3 (cloud CLI + GUI) FATTO**: nuovo `gray_matter/cloud.py` (stdlib,
runner iniettabile) + subcomando CLI `cloud setup|status|teardown`
(`--group/--components/--env-file`). Setup idempotente: whoami → gruppo → 3 DB
(`neuron`/`neurag`/`gm-bridges`) → un solo group token (riusato se già nel
`.env`) → scrive `<GM_HOME>/.env` (backup `.bak`, mai clobber, solo se cambia)
→ report tier. Teardown de-cabla solo le env. GUI: pannello "Cloud group…" in
`webgui.html` + Api `cloud_setup/status/teardown` che streammano la CLI;
`turso_save` ora scrive nel `.env` GM (nota residua chiusa). Test:
`test_cloud_setup.py` (3 test, FakeTurso). Suite sandbox: **43 passed, 3 skip**.
CHANGELOG (Unreleased) + CONFIGURATION.md(.it) aggiornati.
⚠ Da verificare in locale: nome esatto della sottocomando token della turso CLI
(il codice prova `group tokens mint` poi `group tokens create`).

**Passo 4 (install opt-out §6) FATTO**: i 4 launcher (`Neuron|Neurag/install.sh|.ps1`)
ora chiedono `Install Gray Matter (recommended)? [Y/n]` con il warning-deficit;
headless `--no-gm` / `GM_OPTIN=0`; rifiuto → `standalone_install()` (venv proprio,
Neuron si registra con `neuron register --client all`, NeuRAG stampa lo snippet
`neurag-mcp` + doctor); GM non ottenibile → **degrada a standalone** invece di
EXIT (caso simmetrico §6). Reversibile ri-eseguendo senza `--no-gm`.
`sh -n` ok; ⚠ i `.ps1` vanno parse-checkati in locale (niente pwsh in sandbox).

**G3 GUI adattiva + pannello di controllo FATTI**:
- webgui: sezioni con `data-req="neuron|neurag"` nascoste se il componente manca
  (`applyPeers` da `eco_status`) → GUI utilizzabile in ogni combo; Ecosystem box
  sempre visibile = recovery (Install).
- Sidebar completa: Memory (Neuron: overview/doctor/consolidate/visualize/
  register/console) + Knowledge (NeuRAG: status/tree/query/import/health/doctor).
- Orchestrator card: **Full suite** / **Standalone (no bridge)** (= mode
  collaborate/separate) + Stats + Doctor (`gm_stats`/`gm_doctor`).
- **Dashboard**: `panel_info` (best-effort su ogni parte: versioni via
  importlib.metadata, stato daemon, stats IPC, tier cloud, conteggio bridge,
  peers) + pannello `dashForm`. Verificato nel caso peggiore (nessun peer,
  daemon off): non si svuota mai.
- Verifiche: suite GM 43 passed · py_compile ok · JS `node --check` ok · id DOM
  tutti risolti. CHANGELOG × 3 e INSTALL.md(.it) aggiornati.

**AUDIT OpenCode (Plan A+B) FATTO** — vedi `AUDIT-2026-07-21.md`. Esiti chiave:
suite 272 / 36 / 46 tutte verdi; bridge 3-tier, migrazione, `.env` GM, cloud
status/teardown, opt-out standalone+degrade: OK. B5 bridge su Turso cloud: OK.
Fix di OpenCode **propagati ai gemelli** in questa sessione:
- BOM (PS 5.1): `utf-8-sig` in `gray_matter/_env.py` + `cloud.py` (read/update)
  + `neuron/_env.py`; 2 test BOM nuovi → GM suite ora **48** (45+3 skip sandbox).
- Degrade-exit installer: exit solo su successo di `gray-matter install` anche
  in `Neuron/install.ps1` e nei due `.sh` (tolto `exec`).
Nota: `delete_node` conserva il try/finally (lo snippet nell'audit era della
run OpenCode, il file è già hardened); `test_search_with_links_enriches` è già
tier-agnostico e in B1 passa (la nota "pre-esistente" dell'audit è stantia).

**`cloud wire` (BYO senza turso CLI) AGGIUNTO** dopo l'audit: un utente normale
usa Turso come sempre — incolla URL/token dal dashboard, niente CLI. Core
`cloud.wire()` (parziale ok, probe best-effort via `neuron.connect`, probe
fallito → niente scritto, token mai in output); CLI `gray-matter cloud wire
--neuron-url/--neurag-url/--gm-url [--token]` (fallback token: env → .env →
getpass); GUI: sezione "Manual — NO turso CLI" nel pannello Cloud group
(in-process, token mai su argv). Test `test_wire_no_cli` → GM suite **49**
(46+3 skip sandbox). Il vecchio form "Turso setup…" (solo Neuron) resta e
continua a funzionare. `setup` (auto-provisioning) resta per chi ha la CLI.

**Install turso CLI di default (opt-out) AGGIUNTO**: `cloud setup` senza CLI
offre l'install con prompt [Y/n] — installer ufficiale pinnato
(`GM_TURSO_CLI_VERSION` = v0.7.0-pre.22; win: `irm …installer.ps1 | iex`,
posix: `.sh`). Opt-out `--no-cli-install`/`GM_TURSO_CLI_INSTALL=0`, headless
`--yes`; rifiuto/fallimento → `CLI_GUIDE` (manuale, scelta loro). GUI: bottoni
"Install CLI" (confirm) + "Guide" nel pannello. Suite GM **51** (48+3 sandbox).
⚠ Da verificare in locale (OpenCode): l'installer ps1 pinnato gira davvero e
mette `turso` sul PATH; e il nome sottocomando token (B4, ancora aperto).

**B8 verificato da OpenCode** (audit aggiornato): wire ok, opt-out ok, 7/7 e
suite 51 in locale. Suo fix (token orfano mai scritto) **conservato ed esteso**:
ora la *rotazione* token è lecita (token nuovo + URL già nel .env, probe contro
una URL salvata); token senza alcuna URL resta rifiutato. +2 assert in
`test_wire_no_cli` → suite GM **53 in locale** (48+3 skip sandbox).
NOTA per OpenCode: restano da chiudere install CLI reale (B8 ⚠ PATH) solo se
non già coperto, token subcommand (B4), Plan A §9/§10, git/tag ultimo.

**B4 CHIUSO + passi 5-6-7 FATTI** (sessione serale):
- **B4**: comando token = `turso group tokens create` (docs ufficiali; `mint`
  non esiste → rimosso). SCOPERTA: `tursodatabase/turso` v0.7.x (installer
  nativo Win proposto) è il DATABASE LOCALE, non la CLI cloud → install offerta
  = script `get.tur.so` (posix); Windows = guida WSL, con `wire` raccomandato.
  `setup` riconosce la CLI sbagliata sul PATH. +3 test.
- **Passo 5**: doctor esteso — `tiers` dei 3 (env>%GM .env; NeuRAG dall'engine
  live) + `cross_store` (neuron+neurag vivi e collab). CLI stampa entrambi.
- **Passo 6**: stimulus safety-net — contatore turni-tool senza 🧠/⚡; alla
  soglia GM rilancia via `forgotten` sul pass-through. Knob
  `stimulus_safety_net`/`stimulus_safety_gap` (→ GUI Prefs automatica).
- **Passo 7 (G2)**: daemon → `logs/daemon.log` (spawn `-u`, append, marker);
  `gray-matter logs [-n N] [--follow]`.
Suite sandbox: **53** (50+3 skip). Test manuali utente in arrivo; git DOPO.

**FIX dai test manuali utente (post-install reale)**:
1. GUI tutti i comandi rotti → `_exe()` in webgui: i console script si risolvono
   accanto a sys.executable (venv Scripts non è nel PATH del processo GUI).
   Applicato a `_stream`/`_run_seq`/`_open_terminal`.
2. Register Claude Code "cli failed" → su Win `claude` è `.cmd` (CreateProcess
   non lo esegue): `_claude_argv()` = which + `cmd /c`; detail = errore vero.
   ⚠ Ri-testare in locale: se fallisce ancora, ora il report DICE perché.
3. Installer non idempotente a vista → skip con messaggio quando versione
   sorgente == installata (install.sh + install.ps1, GM e peer).
4. Setup card: checkbox default OFF (spunte = aggiungi componenti).
Nota "stato sbagliato in GUI": quasi certamente sintomo del punto 1 (comandi
falliti → dati vuoti); ri-valutare dopo il fix.

**Restano aperti** (non testabili finora): nome sottocomando token turso CLI
(`mint` vs `create` — turso CLI non installata da OpenCode, B4 parziale);
Plan A sez. 9 cloud Neuron/NeuRAG (niente credenziali) e sez. 10 L2 2-client;
git/tag = ULTIMO.

**Prossimi passi = 5–8** (doctor esteso, stimulus safety-net, G2 logs --follow,
verifica cloud reale via OpenCode).

## Stavo facendo PRIMA (sessione precedente — ora chiuso, vedi sopra)
**B-STORE — bridge come store 3-tier** (task #30). Fatto:
- `gray_matter/bridges.py` **riscritto** da JSON → tabella `bridges` (sqlite locale / Turso
  cloud via `GM_TURSO_DATABASE_URL`+token, DB proprio `gm_bridges`). API pubblica invariata
  (`add_bridge`/`bridges_for`/`decay`/`all_bridges`), upsert con incremento relativo
  (`min(weight+1,cap)`) concurrent-safe, migrazione one-shot da `bridges.json`→`.json.migrated`.
  Facade `_RemoteConn` name-accessible (keep-in-sync con Neuron/NeuRAG).
- `gray_matter/paths.py`: `gm_bridges()` → `bridges.db`.

**PENDING (non ancora fatto):**
1. Riscrivere `gray_matter/tests/test_bridge_promotion.py` per usare l'**API** (`all_bridges()`)
   invece di leggere il JSON grezzo; env `GRAY_MATTER_BRIDGES` → path `.db`; `promoted` ora è
   int (truthy, non `is True`). + nuovo `test_migrates_legacy_json_once`.
   (La Write è fallita: serve Read del file prima di sovrascriverlo.)
2. Aggiungere extra `[cloud] = ["libsql-client>=0.3.1"]` a `gray_matter/pyproject.toml`.
3. Girare la suite GM in sandbox (tier sqlite) → attesa verde.

## Fatto in questa sessione (sintesi)
- **P1 versioni** → 6.0.0 / 1.0.0 / 1.0.0 ovunque. Bug reale: `__init__.py` è la SSOT a runtime,
  `__version__.py` era morto → ora re-export. `gray-matter` ora dice v1.0.0.
- **P2 docs**: creati `docs/INSTALL.md(.it)`; tool `status` in TOOLS; "32→33"; marker trust
  B1–B3 nel COMPENDIUM; modello embedding corretto in TECHNOLOGY (multilingual MiniLM-L12,
  non bge). C4 (CLI console/tunnel) era **falso positivo**.
- **P3 toolchain**: `importorskip("mcp")` sui 2 test GM; gap vendor/dev + case NeuRAG documentati
  in `ENVIRONMENT.md §2`.
- **P4**: test `search_with_links` reso **tier-agnostico** (`>=2` + enrichment); design annotato.
- **L2**: guardia `_open_local_engine` (Neuron) / `_open_local_turso` (NeuRAG) = retry + degrade
  a sqlite3; `test_l2_open_guard.py`. Sotto-caso dir-mancante confermato già gestito; residuo =
  race multi-processo (da chiudere sul daemon vivo).
- **W1–W3**: decoupling wheel NeuRAG (`Neurag/vendor/` + `release.yml`); bootstrap installer
  Neuron/NeuRAG (locale→GitHub release→PyPI→EXIT).
- **A1–A3**: flow installer irrobustito — ogni crossing point degrada a fallback; EXIT solo per
  no-Python / no-venv / GM-core-non-installabile.
- **CORE-1**: port cloud Turso in NeuRAG (`RemoteTursoConnection`, righe by-name, extra `[cloud]`).
  **Fix chiave**: NeuRAG usa `NEURAG_TURSO_DATABASE_URL` (DB SEPARATO, non quello di Neuron —
  collidono sulla tabella `nodes`). Token condiviso via `NEURAG_TURSO_AUTH_TOKEN`→`TURSO_AUTH_TOKEN`.
- **ENGINE** reporting NeuRAG allineato (`Turso (cloud)`/`Turso (local)`/`SQLite`).
- **doctor**: aggiunto `neurag doctor` (env+tier+vault); GM/Neuron già ce l'avevano.
- **Verifica locale OpenCode** (Win/Py3.14, deps reali): Neuron **272** · NeuRAG **36** · GM **38**.
  Trovato+fixato da OpenCode un bug reale solo-pyturso: `NeuRAG.delete_node()` stack-overflow C su
  FK CASCADE → `PRAGMA foreign_keys=OFF/ON`; io l'ho **irrobustito con try/finally**.

## Decisioni bloccate (ref DESIGN-CLOUD-MEMORY §Decisioni bloccate)
1) storage bridge = **A** (tabella) · 2) **un solo group token** · 3) piggyback **ON** + **GM rete
di sicurezza stimoli** (rilancia se l'LLM dimentica i tool) + **toggle/tuning in GUI per ogni
funzione** · 4) 3° DB = **`gm_bridges`** · 5) install = **opt-out** (installa GM salvo rifiuto,
con warning-deficit).
Principi: **dual-implementation Neuron↔NeuRAG** dove ha senso; flussi **separati**; **GUI chiama la
CLII** (un solo flusso logico).

## Prossimi passi (ordine, ref DESIGN-CLOUD-MEMORY §Action items)
1. Chiudere B-STORE (i 3 PENDING sopra) → GM suite verde.
2. Env model: `GM_TURSO_DATABASE_URL` + verificare/implementare **propagazione env daemon→worker**
   (oggi Neuron legge il suo `.env`; serve un `.env` a livello GM o iniezione esplicita).
3. `gray-matter cloud setup|status|teardown` (CLI core idempotente: full group / BYO / parziale) +
   **pannello GUI** che la invoca.
4. **Install-choice opt-out** (§6): prompt GM raccomandato + warning-deficit; `--no-gm`/`GM_OPTIN`.
5. **doctor esteso**: tier di tutti e 3 (incluso bridge) + se cross-store è attivo.
6. **Stimulus safety-net**: GM rilancia lo stimolo quando Neuron non emette piggyback (LLM ha
   dimenticato i tool). Toggle in GUI.
7. **G2** `logs --follow` · **G3** GUI adattiva (launcher GM>Neuron>NeuRAG + recovery page).
8. **Verifica cloud reale via OpenCode**: `TEST-OPENCODE §9` con `TURSO_*` (Neuron) e
   `NEURAG_TURSO_*`/`GM_TURSO_*` (DB separati). + **L2 concorrenza** (§10, 2 client).
9. **Git/tag/push = ULTIMO** (dopo tutto verde): tag `v6.0.0`/`v1.0.0`/`v1.0.0` → attiva `release.yml`.

## File toccati in sessione
- Neuron: `src/neuron/__init__.py`, `pyproject.toml`, `README.md`, `CHANGELOG.md`, `src/neuron/db.py`
  (guardia L2), `src/neuron/models.py` (commento embed), `tests/test_l2_open_guard.py` (nuovo),
  `install.sh`, `install.ps1`.
- NeuRAG: `db.py` (cloud port + guardia L2 + engine name + delete_node hardening), `cli.py`
  (doctor), `__init__.py`, `__version__.py`, `pyproject.toml`, `README.md`, `CHANGELOG.md`,
  `vendor/README.md`+wheel, `.github/workflows/release.yml` (nuovo), `install.sh`, `install.ps1`,
  `DESIGN-CROSSLINKS.md`, `tests/test_node_links.py`, `tests/test_cloud_turso.py` (nuovo),
  `tests/test_vector_sql.py`.
- gray_matter: `__init__.py`, `__version__.py`, `pyproject.toml`, `README.md`, `CHANGELOG.md`,
  `install.sh`, `install.ps1`, **`bridges.py` (riscritto)**, `paths.py`, `tests/test_gateway_flip.py`,
  `tests/test_installer.py`.
- Root/docs: `docs/` (INSTALL, TOOLS, TECHNOLOGY, EVOLUTION, OVERVIEW, CONFIGURATION + .it),
  `ENVIRONMENT.md`, `GRAY-MATTER-COMPENDIUM.md`, `PROBLEM-REGISTER-2026-07-21.md`,
  `DESIGN-CLOUD-MEMORY-2026-07-21.md` (nuovo), `TEST-OPENCODE-2026-07-21.md` (nuovo).

## Note ambiente (ref ENVIRONMENT.md)
- Sandbox = tier **sqlite**; verdetto Turso/cloud/L2-concorrenza solo **in locale/OpenCode**.
- Edit via file-tool (path Windows). Shim pytest 3.10 in `/tmp/shims` (exceptiongroup/tomli),
  symlink `neurag` in `/tmp/nrtest`, `/tmp/pytest.ini` — vengono ripuliti tra le run, ricrearli.
- OpenCode ha `.venv-test` con install `-e` → le mie modifiche sono già live da lui.
