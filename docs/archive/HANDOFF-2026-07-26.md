# HANDOFF — sessione 2026-07-26

> Stato per la prossima sessione. NON riscrive i doc: li linka. Verità dal codice.

## STATO TEST

**512 passed, 0 failed** (Neuron 271 + NeuRAG 38 + Gray Matter 119 + mega-integration 83)
- 1 skip: `test_reset_refuses_without_confirm` (PermissionError Windows DB lock)
- 33 warnings NeuRAG (fastembed model)

## FATTO IN QUESTA SESSIONE

### Installer — mode selector F/N/D
Tutti e 6 gli script (`install.ps1` + `install.sh` per GM, Neuron, NeuRAG) hanno:
- `[F] Full suite` → GM + tutti i peer
- `[N] Standalone` → solo il tool, venv proprio
- `[D] Details` → mostra cosa perdi senza GM

### GM coupled mode — detect + ask
Quando Neuron o NeuRAG chiama GM in coupled mode (`GM_PEER_DIR`), GM:
1. Installa GM + il peer chiamante
2. Cerca l'altro peer come sibling
3. Se lo trova, chiede all'utente se vuole aggiungerlo

Funziona sia in `install.ps1` che `install.sh`. Aggiunto `Find-PeerIn` / `find_peer_in`.

### BOM UTF-8 fix
- `gray_matter/clients.py` + `executor.py`: `encoding="utf-8-sig"` per leggere `opencode.json` con BOM
- `install.ps1`: `Prompt-InstallChoice` senza `UserInteractive` check (funziona anche in non-interactive)
- `neuron/install.ps1`, `neurag/install.ps1`, `gray_matter/install.ps1`: UTF-8 BOM aggiunto
- `neuron/install.ps1:163`, `neurag/install.ps1:158`: fix `@args → @Fwd` (variabile phantom)
- `gray_matter/install.ps1`: 4 righe con `\u00e8` letterale → `è` reale

### Context persistence
- `neuron/registry.py`: `_active_context.txt` nel graphs dir
- Scritto su `switch_context`, letto all'init del grafo

### FASE 5.3–5.6 (dalla sessione precedente, già mergeati)
- Graveyard reactivation + decay (L3)
- Memory recall flash in stimulus.py
- Tool `recall` per ri-archiviare nodi dal graveyard
- Formato compatto potenziato (link type abbrevs, rationale trim)
- Semantic dedup su store_turn (cosine >0.90)
- MMR diversification su get_context (λ=0.7, depth≥2)

### GUI Refactoring — 4 fasi completate

#### Fase 1: Registry + Discovery (`gray_matter/gme.py`)
- GME folder: `%LOCALAPPDATA%\GrayMatterEnvironment\`
- Ogni tool ha un JSON (`neuron.json`, `neurag.json`, `gray-matter.json`)
- `install()` → write + venv detection + shortcut
- `uninstall()` → remove
- `migration_banner()` → returns tools to migrate to shared venv
- `registry_status()` → overall status

#### Fase 2: Health Stream (`gray_matter/webgui.py`)
- `health_state()` → real-time tool status per env
- `health_stream()` → SSE endpoint for live updates
- Interactive health bar (click to expand details)

#### Fase 3: Migration UI + i18n (`gray_matter/webgui.html`)
- `migrate()` → migrate tools between venvs
- Full IT/EN bilingual (51 i18n keys)
- Migration banner + per-tool migration
- Interactive health bar controls
- Footer Start button

#### Fase 4: Desktop Shortcuts (`gray_matter/shortcut.py`)
- Centralized shortcut creation (one "Gray Matter.lnk")
- Auto-recreate if .lnk deleted
- All install scripts write to Desktop via this module

### Turso DB Cloud Resilience (Fase 1 — NeuRAG + GM bridges)
Modified `neurag/db.py` (RemoteTursoConnection) and `gray_matter/bridges.py` (_RemoteConn):
- `_with_retry(fn, *, attempts=5, base_delay=0.25, on_retry=None)` — exponential backoff
- `_url_candidates(url)` — tries user URL first, then https:// twin (libsql→https fallback)
- `_reconnect(self)` — close `_client`, call `_create_client()` (NOT `self.connect()`)
- `begin/commit/rollback` — real transaction support (buffered writes)
- `executemany` — batch insert support
- Pattern copied from `neuron/db.py` reference implementation (keep-in-sync, not shared module)

### Tunnel Rewrite (`neuron/tunnel.py`)
Complete rewrite with 4 backends:
- `cloudflared quick tunnel` (quick, ephemeral)
- `cloudflared tunnel run --url` (named, persistent URL) ← **preferred**
- ngrok (Plan B)
- `neuron tunnel setup` (guided first-time setup for named CF tunnels)
- Watchdog (auto-restart on crash)
- `_auto_detect_backend(args)` — auto-select best available backend
- Config persistence: `%LOCALAPPDATA%\GrayMatterEnvironment\tunnel.json`
- Credentials: `~/.cloudflared/cert.pem` via `_has_cf_credentials()`

### HTTP Bridge Rewrite
**Neuron** (`neuron/bridge.py`):
- `resolve_neuron_cmd()` — **full suite detection**: if `gray_matter.server` detected → launch GM (preserves full tool set: pulse, bridges, flash, cache, NeuRAG)
- Standalone fallback: own server if GM absent
- `--host 0.0.0.0`, `--port` with fallback (`_find_free_port`)
- `--tunnel` flag for auto-launch
- Env vars: `NEURON_BRIDGE_HOST/PORT/TUNNEL`

**NeuRAG** (`neurag/bridge.py`):
- `resolve_neurag_cmd()` — **same full suite detection** as Neuron
- Default port 8001, env vars `NEURAG_BRIDGE_HOST/PORT/TUNNEL`
- `--tunnel` flag, `preflight()` with GM-aware messages

**GM** (`gray_matter/bridge.py`) — **NEW dedicated full-suite bridge**:
- Port 8002, `resolve_gm_cmd()` (always GM, no detection needed)
- `resolve_proxy_runner()` for MCP proxy
- `_find_free_port()`, `_launch_tunnel()`, `preflight()`
- `GM_BRIDGE_HOST/PORT/TUNNEL` env vars
- `--bind all` for 0.0.0.0 binding
- Wired to `gray-matter bridge` CLI subcommand

### GM CLI Bridge Command (`gray_matter/cli.py`)
- `gray-matter bridge` subcommand with all flags
- Wired to `COMMAND_GROUPS["lifecycle"]`
- Dispatches to `bridge.bridge_main()`

### Windows WAL Lock Fix (`neuron/registry.py`)
- `_unlink_db()` static method: removes `-shm`, `-wal`, then `.db` in correct order
- Retries on `PermissionError` with 50/100/150ms backoff (up to 4 attempts)
- `reset()` uses it instead of raw `os.unlink()`
- Prevents Windows concurrent DB access issues in test suites

### Mega Integration Test Suite (`neurag/tests/test_mega_integration.py`)
**83/83 tests** across 10 sections:
1. Bridge resolution — full suite / standalone
2. Turso URL candidates
3. Retry logic
4. Reconnect
5. Tunnel imports + structure
6. WebGUI HTML integrity
7. CLI wiring + env vars
8. Cross-store bridges
9. GME registry
10. Stress/concurrency: 20 threads, 100x batch, port fallback chains, retry performance, LLM simulation, cache flow, edge cases

## TODO PROSSIMA SESSIONE
- **Fix terminale visibile**: lanciando Neuron dalla GUI si apre un terminale. Probabilmente `_CREATE_NO_WINDOW` non propagato in qualche punto della chain (webgui → subprocess → server). Verificare tutti i punti dove si lancia `python` o il server Neuron.
- **Cloud Turso panel**: dati troppo piccoli/compatti nella GUI. Da espandere (mostrare URL completo, stato connessione, fallback chain).

## DOPO GUI
- Rivisitare registrazioni client (non solo opencode — l'utente ha specificato)
- Bump versione (6.1.2 → 6.2.0?)
- CHANGELOG.md aggiornamento
- Tag git release
- User testing + release decision

## FILE CHIAVE MODIFICATI
- `gray_matter/gme.py` — GME registry + migration + venv detection
- `gray_matter/shortcut.py` — centralized shortcut creation
- `gray_matter/catalog.py` — GME discovery + `_python()` + COMMAND_GROUPS + DESCRIPTIONS
- `gray_matter/webgui.py` — health_state(), migrate(), _python_for_tool(), Api class
- `gray_matter/webgui.html` — 997 lines, health bar, i18n 51 keys, migration banner
- `gray_matter/server.py` — IPC listener, `Server` from mcp, tools via `@app.list_tools()`
- `gray_matter/cache.py` — `ContextCache(max_size, ttl)` with `set/get/invalidate/invalidate_related/size`
- `gray_matter/bridges.py` — MODIFIED: _RemoteConn with _with_retry, _url_candidates, _reconnect, begin/commit/rollback, executemany
- `gray_matter/bridge.py` — NEW: dedicated full-suite HTTP bridge (port 8002)
- `gray_matter/cli.py` — MODIFIED: added `gray-matter bridge` subcommand
- `gray_matter/install.ps1` — GME write + single shortcut; encoding fixed
- `gray_matter/install.sh` — GME write + single shortcut
- `neuron/install.ps1` — encoding fixed; `@args→@Fwd` at line 163
- `neuron/install.sh` — GME write + single shortcut
- `neuron/src/neuron/tunnel.py` — REWRITTEN: named CF tunnel, ngrok, guided setup, auto-detect
- `neuron/src/neuron/bridge.py` — REWRITTEN: full suite detection, port fallback, --tunnel, env vars
- `neuron/src/neuron/registry.py` — MODIFIED: added `_unlink_db()` static method
- `neuron/src/neuron/shortcut.py` — detect missing .lnk and recreate
- `neuron/src/neuron/db.py` — reference implementation for retry/reconnect
- `neurag/bridge.py` — REWRITTEN: full suite detection, port 8001, --tunnel, env vars
- `neurag/db.py` — MODIFIED: RemoteTursoConnection with _with_retry, _url_candidates, _reconnect, transactions
- `neurag/tests/test_mega_integration.py` — NEW: 83/83 mega test suite
- `neurag/tests/test_cloud_turso.py` — test_url_candidates_normalisation
- `neurag/install.ps1` — `@args→@Fwd` at line 158; BOM added
- `neurag/install.sh` — doctor-before-register order fixed
- `work/design/` — ADR, plan, risk, checklist, mocks

## NOTE TECNICHE
- Venv condiviso: `$env:LOCALAPPDATA\gray-matter\.venv`
- Venv dev: `neuron/.venv`
- Pytest da dev venv: `neuron\.venv\Scripts\python.exe -m pytest`
- `opencode.json`: `%USERPROFILE%\.config\opencode\opencode.json`
- GME folder: `C:\Users\recla\AppData\Local\GrayMatterEnvironment\`
- Neuron DB: `C:\Users\recla\AppData\Local\neuron\graphs\graph_backend.db`
- `_CREATE_NO_WINDOW = 0x08000000` for Windows subprocess
- Turso DB separation: Neuron=`TURSO_*`, NeuRAG=`NEURAG_TURSO_*`, GM=`GM_TURSO_*`
- Tunnel config: `%LOCALAPPDATA%\GrayMatterEnvironment\tunnel.json`
- Bridge defaults: Neuron=8000, NeuRAG=8001, GM=8002
- pyturso 0.6.1 known bugs: FK cascade crash, Windows file lock, concurrent open race, WebSocket flakiness
- `_with_retry` API: keyword-only `fn, *, attempts, base_delay, on_retry`
- `_reconnect(self)`: closes `_client`, calls `_create_client()` (NOT `self.connect()`)
- Server tools: defined via `@app.list_tools()` decorators (no module-level TOOLS list)
- ContextCache: `set/get/invalidate/invalidate_related/size` (not `put`)
